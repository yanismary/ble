import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mantio.app',
  appName: 'Mantion SMT',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
